# Liveliness PRD-08 — Games Visual Uplift: Math

**Date:** 2026-07-17
**Part of:** Games Visual Uplift sub-program (see `tmp-prd-liveliness-06-games-uplift-foundation.md`).
**Depends on:** **PRD-06 Foundation shipped** and **PRD-07 Alphabet shipped**. The Foundation primitives now
exist in-tree and are proven by the alphabet area: `TactileTile`, `PromptFocus`, `TactilePill`, the softened
in-game world veil, the unified HUD (`AnswerTile`/`ScoreChip`/`RepeatButton` all ride the tactile language),
the per-section baked-art loader convention (`src/assets/games/<section>/index.ts` eager-`import.meta.glob` +
a sync `letterArt()`-style helper), and the render hooks `QuizItem.questionVisual.art`→`HeroArt`,
`MemoryItemDisplay.iconArt`, browse bloom via `PromptFocus`. This PRD **consumes** those, unchanged.
**Owner:** Allan (parent). **Target user:** one ~5-year-old boy, iPad-primary, **pre-reader** (counts to
~60–70, adds to 20 on fingers, basic subtraction, knows all letters — see `user_son-age`).
**Order:** SECOND area (Alphabet ✅ → **Math** → Farver → Ordleg → English), authored just-in-time so it
absorbs the alphabet play-test + implementation learnings (folded in below).

> **Art-gated:** this area ships only once its baked soft-3D WebP batch (§4) is generated by the owner, keyed
> via the `.claude/rules/scene-assets.md` pipeline, and dropped into `src/assets/games/math/`. The code work
> (§3) can be built against the manifest first; every consumer keeps its emoji/glyph fallback until the art
> lands (auto-registers, no code change) — exactly as alphabet did.

---

## 0. Alphabet learnings carried forward (fold-in)

Grounded from the shipped alphabet area (`e09f1aa`/`de6b498`/`9a2ba2d`) and its debrief (`696fe52`):

1. **The art loader pattern works verbatim** — a per-section `index.ts` that eager-`import.meta.glob`s `./*.webp`
   (`{ eager, query: '?url', import: 'default' }`), maps each filename stem → key, and exports a sync
   `xxxArt(id)` helper returning `string | undefined`. Empty until files land, then auto-registers. Math copies
   `src/assets/games/alphabet/index.ts` to `src/assets/games/math/index.ts` with number/object-id keys.
2. **`questionVisual.art`→`HeroArt` is the quiz-hero hook.** The default `UnifiedQuizGame` path renders
   `qv.art ? <HeroArt src={qv.art}/> : <HeroEmoji>` (`UnifiedQuizGame.tsx:529-533`); games with a custom
   `renderHero` (Tal Quiz, Hvad Mangler) render `<HeroArt>`/`<HeroEmoji>` themselves. `HeroArt`/`HeroEmoji`
   both live in `PromptStage.tsx` (kept as the home of those helpers; the frosted *card* is what PromptFocus
   retired).
3. **Memory uses a single `iconArt` `<img>`** on the card front (`UnifiedMemoryGame.tsx:769-784`) — the mechanism
   renders exactly one image. Numbers memory wanting a *count* cluster needs a small, backward-compatible engine
   addition (§3.6 / §6), not just data.
4. **Picture-less items stay glyph-only, honestly** (alphabet Q). Math's analogue: **numerals are glyphs and
   never become art**, and abstract pattern tokens (Hvad Mangler's colour dots) are not "depicted subjects" —
   see §3.5 / §6.
5. **Consolidate to ONE shared subject set.** Alphabet merged its two divergent letter→word manifests into one
   so a letter shows the same object everywhere. Math's counting objects must likewise be **one shared set**
   used by Tal Quiz, Lær Tal, Sammenlign, and Memory — so counting "feels" the same across the section.
6. **Screenshot/verify gotcha:** auto-played game TTS usually doesn't fire headless, so identify the showing
   subject via the hero image src (`[data-prompt-focus] img`) + `?seed=<n>`, and drive feedback with
   `?fx=correct|wrong|hint`. (Baked into §8.)

## 1. Context

The Math section is **6 games** off `/math`: **Lær Tal** (calm browse, `/math/numbers`), **Tal Quiz**
(`/math/counting`), **Plus Opgaver** + **Minus Opgaver** (one hand-rolled `MathOperationGame`,
`/math/addition|subtraction`), **Sammenlign Tal** (hand-rolled `ComparisonGame`, `/math/comparison`),
**Hvad Mangler?** (`HvadManglerGame`, `/math/patterns`), and **Hukommelse 10 & 20** (numbers memory,
`/learning/memory/numbers/10|20`). Mechanically they're solid and the owner is happy with how they *teach*
(counting-not-symbol-matching, near-number distractors, the equation-reveal, the krokodille chomp) — the gap
is **look & feel** vs the Structured-World shell (PRD-06 §1).

**What's flat today (grounded this session with `ui-screenshot`, iPad landscape, all games driven):**

| Game | Rides upgraded engine? | What's flat |
|---|---|---|
| **Tal Quiz** | ✅ `UnifiedQuizGame` (PromptFocus + clay tiles) | Only the **counting-object row** for n ≤ 20 is flat 2D emoji (`⭐🔵🟢🍎🎈🐳`); n > 20 shows the bare numeral (correct, glyph). |
| **Hvad Mangler?** | ✅ `UnifiedQuizGame` (PromptFocus + clay tiles) | Numeric sequences are numerals (glyphs, fine); only the **visual-pattern branch** uses abstract colour-dot emoji (`🔴🔵🟢…`). |
| **Plus/Minus Opgaver** | ❌ hand-rolled, renders `PromptStage` directly | The **big white frosted `PromptStage` card** — the Foundation's #1 retired offender — still wraps the equation (very visible over the dark Rummet skin). Answer tiles are already clay. Equation symbols already baked (`SymbolTile`). |
| **Sammenlign Tal** | ❌ hand-rolled, renders `PromptStage` directly | Same **frosted `PromptStage` card** wrapping the arena; the **krokodille is a flat 2D `🐊` emoji**; the **object piles are flat 2D emoji** (`⚽🎈⭐…`). Count-cards are clay; the `>`/`<` is already baked `SymbolTile`. |
| **Lær Tal** | ❌ browse, renders `PromptStage` directly | The **frosted `PromptStage` bloom card**; the counted objects are flat `⭐` emoji (and accent dots for dense counts). Grid cells are already clay (`LearningGrid`→`TactileTile`). |
| **Hukommelse Tal** | ✅ `UnifiedMemoryGame` (clay faces + flip) | Card backs are clay; the matched **front is numeral-glyph-only** (`getDisplayData` returns just `{ primary }`) — no counting reinforcement, no baked icon. |

**The core finding:** three games (Plus/Minus, Sammenlign, Lær Tal) **never adopted PromptFocus** — they were
hand-rolled / used `PromptStage` directly, so the Foundation's engine-level swap didn't reach them. They are
the concentrated code work. Tal Quiz + Hvad Mangler + Memory already have the material; they need only baked
**counting-object art** where they still show flat emoji.

**Scope (owner-set):** numerals stay glyphs; **baked art = counting objects + memory-card icons** (plus the
Sammenlign krokodille, which is the section's one flat-emoji character). Keep `MathOperationGame`'s
**equation-reveal** and `ComparisonGame`'s **krokodille chomp** exactly.

**Success:** entering any math game feels like the Rummet/Regnbue/Havet/Dino menu it launched from — the
equation/comparison/count rests directly in the calm frozen world on its own light-pool (no frosted glass
panel), the things the child counts are tactile soft-3D objects, and the numerals (the lesson) stay crisp
central type. Mechanics, difficulty ranges, audio, and reward flow are untouched.

## 2. Current state (verbatim anchors — branch `feat/games-uplift-alphabet`)

**Tal Quiz — `src/components/math/MathGame.tsx`** (thin `UnifiedQuizConfig`):
- Counting-vs-numeral split: `COUNT_OBJECTS_MAX = 20` (23), `isCountingMode(n) = n <= 20` (24). Counting mode
  HIDES the numeral and shows exactly n objects ("Hvor mange?"); numeral mode (n > 20) shows the numeral alone.
- `renderCountingHero` (59-110): numeral mode → big `<Typography>{n}` (65-78); counting mode → a wrap-flex of
  exactly `n` `<Box component="span">{icon}` where `icon = HERO_COUNT_EMOJI[n % 6]` (81) and
  `HERO_COUNT_EMOJI = ['⭐','🔵','🟢','🍎','🎈','🐳']` (50). **This emoji row is the flat bit → baked art.**
- `renderHero: renderCountingHero` (152); `gameId: 'math.counting'` (169); `round { length:8, three:0, two:2 }`
  (170); `hintAfterNWrong: 2` (173). Distractors = digit-swap + off-by-one/ten (`generateOptions`, 128-148).

**Plus/Minus Opgaver — `src/components/math/MathOperationGame.tsx`** (hand-rolled, props `{operation}`):
- Still imports + renders **`PromptStage`** (`:10`, `:460`) into GameShell's `promptStage` slot with
  `chargeKey={`${num1}-${num2}-${round.state.index}`}` (462) and a `MathRepeatButton` (465).
- The equation is a **hand-styled `<Box>`** inside PromptStage (469-544): `background: tileSurface(accent, dark)`,
  `borderRadius: 4`, `border: 2px solid ${category.borderColor}`, `boxShadow: customShadows.pop` (476-481) —
  numerals `<Typography sx={numberSx}>` + `<SymbolTile op={operator|'='}>` (already baked art).
- **Equation-reveal (KEEP):** the trailing `?` `SymbolTile` flips to the answer via `AnimatePresence` +
  `motion.POP` on `effectiveRevealAnswer` (493-543), success-tinted box (509-528). Driven by `revealAnswer`
  set on a correct tap (347).
- Answer tiles: `AnswerTile` (602-625) with `<Typography>{option}` — already clay via the Foundation.
- Round/audio/advance-lock all intact (`round` 80; `speakCorrectFact`-style completed-fact spoken inline
  331-333; `DWELL_CORRECT` advance 357-372).

**Sammenlign Tal — `src/components/math/ComparisonGame.tsx`** (hand-rolled):
- Renders **`PromptStage`** directly inside a top-anchored layout (`:7`, 532-537) — "the count-cards ARE the
  answers" (comment 39-41). `chargeKey` = the two numbers + round index (535).
- `OBJECT_TYPES` (43-52): 8 `{name,emoji,danishName}` (`🍎🎈⭐🌸🚗⚽🐦🐟`). `renderSide` (408-469) draws a
  fixed-height **flat-emoji pile** of exactly `num` objects (435-446, `emojiPileSx` 64-72) + the numeral
  `<Typography>` (449-461) + `DANISH_NUMBERS[num] + obj.danishName` label (462-464), all inside an `AnswerTile`
  (already clay). **The emoji pile → baked counting-object art.**
- **Krokodille chomp (KEEP):** a flat **`🐊` emoji `<Box component={motion.div}>`** (544-562) that lunges
  `x:[0,±24,0], scale:[1,1.25,1]` (`motion.BOUNCE`) toward the bigger side on `effectiveMouthOpen`, plus the
  `>`/`<` `SymbolTile` springing in (573-590). `sfx.play('chomp')` (350). **The `🐊` → baked art; the motion +
  the `>`/`<` SymbolTile stay.**
- Round/audio/advance-lock intact; completed-fact spoken inline ("sytten er større end ni", 334).

**Hvad Mangler? — `src/components/math/HvadManglerGame.tsx`** (thin `UnifiedQuizConfig`):
- Builds numeric sequences (`buildNumberSequence`, 35-42) or a **visual repeating pattern** from
  `PATTERN_EMOJIS = ['🔴','🔵','🟢','🟡','🟣','🟠','⭐','❤️']` (21) via `buildVisualPattern` (44-54).
- `questionVisual: { emoji:'', word: display }` (108/117); `renderHero` (171-229) splits `word` into tokens and
  renders each as a `<Box component={motion.span}>` — numerals/emoji as-is, the `❓` token boxed + pulsing
  (191-208). `speakCorrectFact` reads the completed numeric sequence (239-246); `skipFirstPrompt: true` (167);
  `hintAfterNWrong: 2` (163); `gameId: 'math.patterns'` (159).

**Lær Tal — `src/components/math/NumberLearning.tsx`** (browse, own component):
- Renders **`PromptStage`** directly (`:10`, 190) with `chargeKey={currentIndex}`. Bloom = numeral
  `<Typography>` (202-218) + `<ObjectCount n={numbers[currentIndex]} accent={MATH_ACCENT}/>` (219).
- `ObjectCount` (29-61): renders exactly `n` items — flat `⭐` emoji up to n ≤ 30, then plain accent **dots**
  (`useDots = n > 30`, 30) sized down as n grows (31). **The `⭐` emoji → baked counting object; the dots stay
  for dense counts.**
- `LearningGrid` (225-231) — cells already clay via the Foundation. `useBrowseXp('math')` (83);
  `awardBrowseXp(String(number))` in `goToNumber` (139). `guide={false}`, `dense` (156-157). No `intro` prop
  (already dropped when the Foundation shipped).

**Hukommelse (numbers) — `src/components/learning/MemoryGame.tsx`** → `UnifiedMemoryGame`:
- `numbersConfig` (82-112): `gameId: memory.numbers.${boardPairs}`; `generateItems: shuffle(NUMBERS)`
  (`NUMBERS` = "1".."20", 17); `getDisplayData(number) → { primary: number }` (94-98) — **no `secondary`, no
  `icon`, no `iconArt`** (numeral-only front). `starThresholds` 10→`{9,18}`, 20→`{18,34}` (34).
- Card front renders `iconArt` as a single `<img>` when present, else `icon` emoji, else nothing
  (`UnifiedMemoryGame.tsx:769-794`). The letters config already passes `iconArt: letterArt(letter)` (11, 55).

## 3. Technical design (per game — consumes the Foundation + PRD-07 loader)

**From the shipped tree (assume these exist, unchanged):** `TactileTile`, `PromptFocus` (props
`{accent, chargeKey?, subject, repeat?, sx?}` — in-world subject on a light-pool + soft contact shadow +
floated "Hør igen", no panel/`backdrop-filter`), `HeroArt`/`HeroEmoji` (`PromptStage.tsx`), `SymbolTile`
(baked operator art), the softened in-game veil, `AnswerTile`/`ScoreChip`/`MathRepeatButton` on the tactile
language, `useReducedMotion`, `sfx`, `celebrateTier`, `mascotBus`.

### 3.1 Baked-art loader (math)
- Create `src/assets/games/math/index.ts` **by copying `src/assets/games/alphabet/index.ts`** and renaming:
  eager-`import.meta.glob('./*.webp', { eager, query:'?url', import:'default' })`, map filename stem → key,
  export `mathArt: Record<string,string>` + a sync `countingObjectArt(id): string | undefined` (and, if the
  crocodile lives here, it's just another key). No ASCII aliases needed (object-id + `crocodile` keys are
  filesystem-safe). Art-gated: empty until the batch lands; every consumer falls back to today's emoji.
- **One shared counting-object set** keyed by stable id (see §4), reused by Tal Quiz, Lær Tal, Sammenlign, and
  Memory — so the objects the child counts are identical across the section (alphabet-consolidation learning §0.5).

### 3.2 Tal Quiz (`MathGame.tsx`)
- **No mechanics/config change.** In `renderCountingHero`'s counting-mode branch (81-107), replace the
  `HERO_COUNT_EMOJI[n % 6]` `<span>` with a baked `<HeroArt>`-style `<img>` of a counting object chosen the
  **same deterministic way** (`objectIds[n % objectIds.length]` — never `Math.random`, to preserve `?seed`
  determinism, per the existing comment 46-49). Keep the shrink-with-count sizing (`heroObjectFontSize`→an
  equivalent px size for the `<img>`). Fallback to the emoji when `countingObjectArt(id)` is `undefined`
  (art-gated). Numeral mode (n > 20) is unchanged (bare numeral — glyph).
- The subject stays inside the existing `PromptFocus` (the engine already renders it) — no card change here.

### 3.3 Plus/Minus Opgaver (`MathOperationGame.tsx`) — retire the frosted card
- **Swap `PromptStage` → `PromptFocus`** (same GameShell `promptStage` slot): `subject` = the equation `<Box>`,
  `repeat` = the `MathRepeatButton`, `accent`/`chargeKey` unchanged. Delete the `PromptStage` import.
- **Re-material the equation "number sentence"** so it reads as clay resting in the world, not a bordered glossy
  pill on a frosted panel: drop the hard `border: 2px solid` + `tileSurface`+`pop` combo (476-481) in favour of
  the tactile material (a soft `TactileTile variant="chip"` / `TactilePill` surround, or the shell's
  `softShadow`/`contactShadow` on a lightly-tinted rounded `<Box>`). Keep the `SymbolTile` operators and the
  big numerals. On the dark skin keep the white numerals (`numberSx`, 422-431) — verify legibility over the
  light-pool instead of the old blurred panel.
- **KEEP the equation-reveal exactly** (the `?`→answer `AnimatePresence`/`motion.POP` flip, 493-543) — it's the
  bespoke post-correct animation the owner wants preserved; only its container material changes.
- Answer tiles unchanged (already clay). Round/audio/advance-lock/`DWELL_CORRECT` untouched. **No baked art** in
  this game (numerals = glyphs; operators already baked `SymbolTile`).

### 3.4 Sammenlign Tal (`ComparisonGame.tsx`) — retire the card, bake the pile + croc
- **Swap `PromptStage` → `PromptFocus`** in the top-anchored arena (532-537); keep the `chargeKey` + repeat.
- **Bake the object piles:** in `renderSide` (435-446) render exactly `num` baked counting-object `<img>`s
  instead of `obj.emoji`, using the same shared set as Tal Quiz. `OBJECT_TYPES` keeps its `name`/`danishName`
  (the spoken "fire bolde" must stay honest) but gains a stable **art id** per entry; fall back to the emoji
  when art is absent. Keep the fixed-height shrink-to-fit pile (must still show all `num` without clipping —
  drag-rule "the shown quantity must match the numeral", §2 comment 59-63) — size the `<img>` like `emojiPileSx`.
- **Bake the krokodille:** replace the `🐊` emoji (561) with a baked soft-3D crocodile `<img>` inside the same
  `motion.div` (keep the lunge `x/scale` `BOUNCE` + `sfx('chomp')`) — **the chomp animation is preserved**; only
  the character art changes. The `>`/`<` `SymbolTile` (580) is already baked — unchanged.
- Round/audio/advance-lock/completed-fact intact.

### 3.5 Hvad Mangler? (`HvadManglerGame.tsx`)
- **Numeric sequences: no art** — they are numerals (glyphs) + a boxed pulsing `❓`; already correct in
  PromptFocus. Leave as-is.
- **Visual-pattern branch:** the `PATTERN_EMOJIS` colour dots (`🔴🔵🟢…`) are **abstract pattern tokens, not
  depicted subjects** — per the glyph/depicted-subject rule (§0.4) they are **not** baked-art candidates.
  **Recommendation (§6):** leave them as-is, OR (optional polish) render each token as a small solid-colour
  **clay pip** (a tiny `TactileTile`-material dot) so the pattern reads tactile without inventing art — a pure
  styling tweak in `renderHero` (185-226), no manifest. Either way, no new WebP.

### 3.6 Hukommelse (numbers) (`MemoryGame.tsx` + `UnifiedMemoryGame.tsx`)
- The matched front reinforces **count ↔ numeral** with the shared counting object via a **count cluster**
  (owner-locked, §6.2): extend `MemoryItemDisplay` with an optional `iconCount?: number` and, when set with
  `iconArt`, render exactly `iconCount` copies of the `<img>` (a mini `ObjectCount`, reusing the shrink-to-fit
  sizing) — numbers pass `{ primary:number, iconArt: countingObjectArt(id), iconCount:n }`; alphabet keeps
  `iconCount` absent → the single image it renders today (backward-compatible, one small `UnifiedMemoryGame`
  change at 769-784). Cap/scale for legibility on the small face (tuning lever; e.g. for n > 10 shrink hard or
  show numeral-prominent + a capped cluster).
- Numerals stay the `primary` glyph. `starThresholds`/round/flip unchanged.

### 3.7 Lær Tal (`NumberLearning.tsx`)
- **Swap `PromptStage` → `PromptFocus`** for the bloom (190); keep `chargeKey={currentIndex}` + the
  numeral + `ObjectCount`.
- In `ObjectCount` (29-61) render baked counting-object `<img>`s instead of `⭐` for the small-count branch
  (n ≤ 30), reusing the shared set; **keep the plain accent dots for dense counts** (`useDots = n > 30`, 30) —
  a baked object at <10px is mush, dots stay legible (this shrink-to-dots logic is already the right call and
  stays). Fall back to `⭐` emoji when art is absent.
- `LearningGrid` cells already clay. Browse-XP/audio/`guide={false}`/`dense` unchanged.

### 3.8 Per-game UX rework?
Math is mechanically sound — the rework is **material + baked art + retiring the three frosted `PromptStage`
cards**, not layout surgery. No mechanic/flow/difficulty changes. (If play-testing shows the lip-less clay tiles
read as less pressable, apply the Foundation's subtle-edge fallback — F1 note — as alphabet did.)

## 4. Baked-art manifest (the pictorial subjects → soft-3D WebP)

Theme-constant soft-3D WebP under `src/assets/games/math/`, keyed by content id. Generate in Gemini on
`#00FF00`, key by **green-excess** + trim + square-contain per `.claude/rules/scene-assets.md`, ≤40 KB square
transparent, style = PRD-05 §8.2 (upload existing `art-src/icons/*.png` — esp. `math.png` — as style refs so
math art matches the section objects and the alphabet set). Full self-contained prompts live in the companion
`plans/liveliness-overhaul/math-art-prompts.md` (produced this session per Foundation F6).

**Counting objects — the shared set (8 subjects).** Keyed to match `ComparisonGame`'s `OBJECT_TYPES` so the
spoken plural ("fire **bolde**") stays honest; Tal Quiz rotates over the same 8 by `n % 8`; Lær Tal uses `star`
(or rotates); Memory uses the count cluster. These are *manipulatives to count* — simple, rounded, instantly
countable at small size.

| id | Danish (plural) | today's emoji | subject |
|---|---|---|---|
| `apple` | æbler | 🍎 | a shiny red apple |
| `balloon` | balloner | 🎈 | a round balloon on a short string |
| `star` | stjerner | ⭐ | a plump five-point star |
| `flower` | blomster | 🌸 | a simple round flower |
| `car` | biler | 🚗 | a chunky rounded toy car |
| `ball` | bolde | ⚽ | a simple play ball |
| `bird` | fugle | 🐦 | a tiny round friendly bird |
| `fish` | fisk | 🐟 | a round cartoon fish |

**Character — the krokodille (1 subject).** `crocodile` — a friendly, cute soft-3D crocodile, side profile,
gentle rounded snout, **no sharp teeth**, calm (it "chomps" toward the bigger number via motion; the art itself
is static + child-safe).

**Numerals: NOT art** (glyphs — the lesson). **Operators `+ − = ? > <`: already baked `SymbolTile`** (not in
this manifest). **Hvad Mangler pattern dots: not art** (abstract tokens — §3.5).

**Gemini subject lines (canonical — full prompts in the companion doc):** apple · balloon · star · flower ·
toy car · ball · little bird · fish · friendly crocodile. Each: *"A single friendly [SUBJECT], soft-3D
claymation / Pixar-lite, rounded matte clay, soft top-left key + rim light, soft contact shadow, warm &
child-safe, slight 3/4 top-down, single centered subject, no text, flat solid #00FF00 background edge-to-edge,
1:1"* + PRD-05 §8.2. Counting objects should read cleanly at small size (they'll be shown up to ~20–30 at once).

## 5. Danish copy
**No new strings, no new narration.** All prompts/echoes/labels reused verbatim ("Hvor mange?",
"Tryk på det største tal", the completed-fact lines "tre plus fire er syv" / "sytten er større end ni", the
`DANISH_NUMBERS` + `danishName` pile labels, "Hør igen"). **No prebake/`/audit` cycle** — nothing narrated
changes.

## 6. Decisions (owner-locked 2026-07-17)
1. **Counting-object set** — ✅ **the 8 in §4** (matched to `OBJECT_TYPES` so plurals stay honest). Tal Quiz
   rotates by `n % 8`; Sammenlign uses ≥2 distinct types per problem; Lær Tal uses `star`; Memory the cluster.
2. **Numbers-memory card icon** — ✅ **count cluster** (§3.6: numeral + exactly-n objects via the small
   backward-compatible `MemoryItemDisplay.iconCount` addition). Not the single-object or glyph-only variants.

**Still open (minor — recommendations stand; decide at implementation):**

3. **Hvad Mangler visual-pattern tokens** — leave the colour-dot emoji as-is (recommended — abstract, not
   depicted) vs the optional clay-pip styling tweak (§3.5). No baked art either way.
4. **Lær Tal dense counts** — keep the accent dots above n = 30 (recommended; baked art at <10px is mush) vs
   push baked objects higher.

## 7. Files to touch
**Create:** `src/assets/games/math/index.ts` + the WebP set (§4).
**Edit:**
- `src/components/math/MathGame.tsx` — counting-hero emoji → baked `<img>` (§3.2).
- `src/components/math/MathOperationGame.tsx` — `PromptStage`→`PromptFocus`, re-material the equation card, keep
  the reveal (§3.3).
- `src/components/math/ComparisonGame.tsx` — `PromptStage`→`PromptFocus`, baked piles + baked krokodille, keep
  the chomp (§3.4).
- `src/components/math/NumberLearning.tsx` — `PromptStage`→`PromptFocus`, `ObjectCount` emoji → baked `<img>`
  (dots stay for dense) (§3.7).
- `src/components/learning/MemoryGame.tsx` — numbers `getDisplayData` → `iconArt` + `iconCount:n` (§3.6).
- `src/components/common/UnifiedMemoryGame.tsx` — add the `iconCount` cluster render (backward-compatible; alphabet unaffected) (§3.6).
- *(if §3.5 polish)* `src/components/math/HvadManglerGame.tsx` — clay-pip pattern tokens.
**Reuse:** `PromptFocus`, `HeroArt`, `TactileTile`/`TactilePill`, `SymbolTile`, `countingObjectArt`,
`getCategoryTheme('math')`, `softShadow`/`contactShadow`, the scene-assets keying pipeline.
**Deprecate check:** after Plus/Minus, Sammenlign, and Lær Tal drop `PromptStage`, grep whether any caller of
`PromptStage` (the card) remains app-wide — if none, it can be removed (keep `HeroArt`/`HeroEmoji`, which some
imports still need; move them if the file goes).

## 8. Verification
- `npm run build` + `npm run lint` clean; `npm run dev` + `dev:api` (**Windows PowerShell**, never WSL).
- `ui-screenshot`, iPad viewport (1180×820) + phone (844×390, 667×375), **all 4 skins**
  (`?theme=kid|ocean|space|dino`), `?nogate=1`:
  - **Tal Quiz:** force a counting-mode number with `?seed=<n>` (n ≤ 20 → object row) — the row is baked soft-3D
    objects, not emoji; verify exactly n render and shrink tidily; n > 20 shows the bare numeral. Identify the
    subject via `[data-prompt-focus] img` (auto-TTS won't fire headless — §0.6).
  - **Plus/Minus:** **no frosted `PromptStage` card / no `backdrop-filter`** in the focal zone (grep + inspect);
    the equation rests as clay on the light-pool in the frozen world; drive `?fx=correct` → the reveal flip
    still lands (POP), `?fx=wrong` → shake + retry. Check the **dark Rummet skin** especially (the old card was
    ugliest there) and for **no white-flash**.
  - **Sammenlign:** no frosted card; the object piles are baked objects (exactly `num`, unclipped, both sides
    distinct types); `?fx=correct` → the **baked krokodille still lunges/chomps** toward the bigger side + the
    `>`/`<` springs in; `sfx('chomp')` wired.
  - **Hvad Mangler:** numeric sequences unchanged (glyphs + pulsing `❓`); the pattern branch renders (dots or
    clay pips per §6.3).
  - **Lær Tal:** no frosted bloom card; small counts (n ≤ 30) are baked objects, dense counts (n > 30) are
    accent dots; grid cells clay; browse XP still ticks on a new number.
  - **Hukommelse Tal:** matched front shows the numeral + baked count cluster (or the chosen §6.2 option); flip
    intact; card backs clay.
  - **Reduced-motion** (`?reduce=1` + OS media): motion off (reveal = instant swap, chomp = instant, no
    idle-float); art + contact shadows + light-pools + audio all intact.
  - Snapshot `localStorage['bornelaering-progress']` before/after a driven round to confirm the reward flow
    (stars/bests) is unchanged.
- 0 console errors / page exceptions on every route. Then **play-test with the son** before authoring `-09` Farver.

## Appendix A — verbatim signatures (math-specific)
Exact per-game render sites + line anchors are captured in §2 (branch `feat/games-uplift-alphabet`). Shared
primitive APIs (`TactileTile`, `PromptFocus`, `AnswerTile`, `HeroArt`/`HeroEmoji`, `SymbolTile`,
`UnifiedQuizConfig`/`QuizItem` incl. `questionVisual.art`, `MemoryItemDisplay` incl. `iconArt`) are in PRD-06
Appendix A + the shipped tree. Key shipped anchors this PRD leans on:
- `UnifiedQuizGame.tsx:66` `questionVisual?: { emoji?; word?; art? }`; `:529-533` `qv.art ? <HeroArt> : <HeroEmoji>`.
- `PromptStage.tsx` exports `HeroEmoji` + `HeroArt` (the frosted *card* is retired; these helpers stay).
- `UnifiedMemoryGame.tsx:74-78` `MemoryItemDisplay { primary; secondary?; icon?; iconArt? }`; `:769-794` single
  `iconArt` `<img>` render (the `iconCount` cluster of §3.6a extends this).
- `src/assets/games/alphabet/index.ts` — the loader to copy for `math/index.ts`.

---
*End of Math PRD. Build against §4's manifest; ship art-gated; play-test, then author `-09` Farver JIT.*
